# Dependencias do estagio `quality` da imagem. Nunca entram no `runtime`.
#
# Instaladas sobre as de execucao porque a suite exercita a aplicacao real:
# cabecalhos, CSRF e autenticacao sao verificados pelo cliente de teste do
# proprio Flask, nao por um duble.

-r requirements.txt

ruff>=0.12,<1
pytest>=8.3,<9

# Usa o armazenamento de certificados do Windows ao rodar fora do container.
truststore>=0.10.4,<1
